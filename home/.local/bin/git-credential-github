#!/bin/bash
# Git credential helper for GitHub using pass

if [ "$1" = "get" ]; then
    if [ -z "$GITHUB_TOKEN" ]; then
        GITHUB_TOKEN=$(pass GitHub)
    fi
    echo "password=$GITHUB_TOKEN"
fi
